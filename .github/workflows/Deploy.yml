name: Deploy Selectivo a Hugging Face

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Descargar TODO de GitHub
        uses: actions/checkout@v4

      - name: 🎯 Crear paquete selectivo
        run: |
          echo "Creando paquete limpio para HF..."
          mkdir -p paquete_hf
          cp app.py paquete_hf/
          cp requirements.txt paquete_hf/
          echo "✅ Paquete creado. Contenido:"
          ls -la paquete_hf/

      - name: 📤 Enviar solo lo necesario
        run: |
          echo "Enviando a Hugging Face..."
          cd paquete_hf
          git init
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "Deploy selectivo $(date)"
          git push --force https://${{ secrets.HF_TOKEN }}@huggingface.co/spaces/LedaRojo/RAG_OpenAI_Python/tree/main
          echo "✅ Solo app.py y requirements.txt enviados a HF"



    

      